{
  "name": "sandbox",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "browser-sync start --server './app' --files 'app/**/*html, app/**/*.css, app/**/*.js, app/**/*.svg, app/**/*.png, app/**/*.jpg, app/**/*.gif'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.23.7",
    "gulp": "^4.0.2",
    "gulp-ruby-sass": "^3.0.0"
  },
  "dependencies": {
    "wicg-inert": "^2.0.0"
  }
}
